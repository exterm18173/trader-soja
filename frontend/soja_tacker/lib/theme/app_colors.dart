import 'package:flutter/material.dart';

class AppColors {
  // Marca (agro/finance)
  static const seedGreen = Color(0xFF0F5E4D); // verde profundo (primária)
  static const seedGreen2 = Color(0xFF17A673); // verde acento
  static const amber = Color(0xFFF2B705); // destaque (alerts/atenção)

  // Neutros (claros)
  static const lightBg = Color(0xFFF6F7F9);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightSurface2 = Color(0xFFF1F3F6);
  static const lightBorder = Color(0xFFE0E5EC);

  // Neutros (escuros)
  static const darkBg = Color(0xFF0B0F14);
  static const darkSurface = Color(0xFF111824);
  static const darkSurface2 = Color(0xFF151F2E);
  static const darkBorder = Color(0xFF263245);

  // Texto (não precisa usar direto se preferir colorScheme)
  static const lightText = Color(0xFF0E1726);
  static const darkText = Color(0xFFE9EEF7);
}
